{
  "name": "github-actions-decrypt-secrets",
  "version": "1.1.0",
  "description": "Decrypt secrets from a JSON file using gpg",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist/main', { recursive: true, force: true }); require('fs').rmSync('dist/post', { recursive: true, force: true })\" && rollup -c",
    "lint": "eslint .",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zgosalvez/github-actions-decrypt-secrets.git"
  },
  "keywords": [
    "actions",
    "node"
  ],
  "author": "Zennon Gosalvez",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.0.0",
    "@actions/io": "^3.0.2",
    "md5": "^2.3.0"
  },
  "devDependencies": {
    "@rollup/plugin-alias": "^5.1.1",
    "@rollup/plugin-commonjs": "^28.0.9",
    "@rollup/plugin-node-resolve": "^16.0.3",
    "eslint": "^10.1.0",
    "rollup": "^4.60.0"
  }
}
